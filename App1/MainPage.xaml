<Page
    x:Class="App1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{StaticResource TestDataContext}">
    <StackPanel x:Name="sta" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="TestTextBlock" Text="asdfghngb"
                   FontSize="{Binding OneSettings.OneFontSize,Mode=TwoWay}"/>
        <Button Content="Test" />
        <Slider LostFocus="slider_LostFocus" x:Name="slider" Value="{Binding ElementName=TestTextBlock,Path=FontSize,Mode=TwoWay}"/>
        <TextBlock x:Name="TestSettings" Text="SRGBFGBSFGN,S" FontSize="{Binding OneSettings.OneFontSize}" />
    </StackPanel>
</Page>
